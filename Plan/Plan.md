I need a vite react pnpm application. Both light and dark mode. Light mode is default.

This is a client side CAD Tool.
It will be called "Track Maker"
The overall goal is to build a track and 3D Print it. Meaning this will need the ability to export selected objects or all objects to OBJ, STL, 3MF.
I already have the foundational dimensions for the starting track. I've designed them in CAD Fusion and 3D printed as tests. This contains the starting track (hot wheels) and the connector. I can provide screenshots in draft mode from all angles.

I will need a choice for the type of track to build. Like Cars, Marble, etc. For the Start the top will Car Track.

A new project should show something like a 3D CAD Modeling environment. I will provide a directory with images and screenshots of the intial design.

One of the main features I need is the showing the track wrapped by the Print Area. So let's say I have 10 pieces of track connected. I want a preview tool that displays a 3D box with platform around each track. If the track extends the box, then add another box. This will give me a visual understanding of how big the actual track is.

I need a way to turn on and off the grid view.
I need a way to add color to the track and connectors.

As shown in the screenshots in the /Plan/media/ folder. The track width should be set to single. Next size up is double the track width but the middle parts are removed. This is like two track parts side by side and then the walls are removed. So I want that functionality. This applys to any size of track. As shown in the example 3, 4, etc.

I will need a way to change the length of the track. The connector ends will stay the same width.

I will also need a track that is a rounded corner. This should be in angles or 45 degrees. There might need to be shorter connector for the corner angles. This will need to be modeled and tested.

I will also need the ability to add in a car that applies gravity and can roll.

Before adding in new track I want to first have to select the end of the track to it into. This will be a toggle feature. If this is toggle off, then the track will be added separately away from the stage.

I want a connector and disconnector tools. Selecting this tool will allow me to disconnect the track end and freely move the tool around in the 3D Space. Each track end will select a small section on both ends when the connector tool is active. Make it a lighter color.

When I select a track part that is not connected I should be able to rotate and move it from the either the Side A, Side B, or the Middle of the of it.

If track is not connected it should be able to flow in 3D space without falling.

Set the connectors default color to blue like in the picture and the track default color to orange.
